---
name: report-builder
description: "Собирает статусы, KPI и артефакты из JSON-результатов шагов обработки в сводный отчёт; различает warning, error и неизвестный статус. Применяется для итога по нескольким шагам."
metadata:
  source: Academy of Yandex AI Studio
  shelf: Аналитика
  type: shell
  role: Аналитик отчётности
  tools: [shell]
  version: "0.2.0"
inputs: [manifest]
outputs: [executive_report]
---

# Сводный KPI-отчёт

Вход — JSON-объект шага, массив шагов или объект `steps`. Основные поля: `skill`, `status`, `metrics`, `outputs`; поддерживаются `name`/`step`, `state`, `kpis`, `artifacts`. Метрики могут быть словарём или списком name/value. Для другой схемы подготовь копию с явным соответствием, сохрани названия KPI, единицы и исходные значения. Не выдумывай отсутствующий статус.

```bash
python3 <skill-dir>/scripts/build_report.py --manifests /app/step1.json /app/step2.json --out /app/executive_report.md
```

`ok` — успех; `warning` — результат с ограничениями; `error` — сбой; `unknown` — недостаточно данных. Warning не повышается до error. Итог учитывает самый существенный статус; отдельно покажи число шагов каждого типа. Наличие `error` в названии бизнес-метрики не означает сбой обработки. Файлы CSV не являются manifest: сначала согласуй нужные KPI или подготовь JSON-сводку по явному запросу.

## Данные и выполнение

Работай с текстом пользователя и загруженными файлами. Доступ к CRM, почте, 1С и другим корпоративным системам не требуется и не подразумевается.
Найди фактические файлы в контейнере; пути из примеров замени на реальные. Путь к скрипту определяется относительно прочитанного `SKILL.md`, а рабочие копии и результаты сохраняй отдельно в `/app` или `/tmp`.
Сначала посмотри заголовки и несколько строк. Если структура отличается, используй параметры сопоставления или подготовь нормализованную копию, показав соответствие полей. Не заменяй отсутствующие или нераспознанные значения нулями.
CSV читается как UTF-8, XLSX — локальным openpyxl из комплекта. Старый XLS требует экспорта в XLSX/CSV; смена расширения не конвертирует файл. Если вложение недоступно, сообщи, какой файл не найден; не анализируй вымышленные данные.
После запуска прочитай созданные файлы, сверь числа с входом и выдай краткий результат со ссылками на существующие артефакты. `manifest.json` содержит метрики и статус для следующего шага.

## Примеры

См. [examples.md](examples.md).

---
name: data-profiler
description: "Проверяет качество CSV/XLSX: детерминированно определяет типы колонок, пропуски, дубли и выбросы; формирует Markdown/HTML-отчёт. Применяется для первичного изучения выгрузки."
metadata:
  source: Academy of Yandex AI Studio
  shelf: Аналитика
  type: shell
  role: Data Analyst
  tools: [shell]
  version: "0.2.0"
inputs: [dataset]
outputs: [profile_report]
---

# Профайлинг данных

```bash
python3 <skill-dir>/scripts/profile.py /app/data.csv --out /app/profile.md --html /app/profile.html
```
Тип вычисляется по всем непустым значениям. Дата без времени — `date`; явное время (включая 00:00) — `timestamp`; в XLSX учитывается формат ячейки. Форматы: YYYY-MM-DD, DD.MM.YYYY, DD/MM/YYYY, YYYY/MM/DD и ISO-время. Слеши трактуются как день/месяц/год. При неоднородных или невалидных значениях колонка может стать categorical — покажи примеры, не угадывай тип заново.
Покажи типы из отчёта и manifest, статистику, дубли и проблемы качества. Идентификаторы из цифр могут выглядеть числовыми; смысл колонки уточняется по данным, а не по случайной оценке модели.

## Данные и выполнение

Работай с текстом пользователя и загруженными файлами. Доступ к CRM, почте, 1С и другим корпоративным системам не требуется и не подразумевается.
Найди фактические файлы в контейнере; пути из примеров замени на реальные. Путь к скрипту определяется относительно прочитанного `SKILL.md`, а рабочие копии и результаты сохраняй отдельно в `/app` или `/tmp`.
Сначала посмотри заголовки и несколько строк. Если структура отличается, используй параметры сопоставления или подготовь нормализованную копию, показав соответствие полей. Не заменяй отсутствующие или нераспознанные значения нулями.
CSV читается как UTF-8, XLSX — локальным openpyxl из комплекта. Старый XLS требует экспорта в XLSX/CSV; смена расширения не конвертирует файл. Если вложение недоступно, сообщи, какой файл не найден; не анализируй вымышленные данные.
После запуска прочитай созданные файлы, сверь числа с входом и выдай краткий результат со ссылками на существующие артефакты. `manifest.json` содержит метрики и статус для следующего шага.

## Примеры

См. [examples.md](examples.md).

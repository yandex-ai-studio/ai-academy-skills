---
name: contract-review
description: "Предварительно проверяет текст договора: выделяет условия, цитаты, возможные риски и вопросы юристу. Применяется для подготовки к юридической проверке; не даёт юридического заключения."
metadata:
  source: Academy of Yandex AI Studio
  shelf: Legal & Compliance
  type: shell
  role: Юридический аналитик
  tools: [shell]
  version: "0.3.0"
inputs: [contract_text]
outputs: [contract_memo]
---

# Предварительная проверка договора

Вход — текст договора или текст, извлечённый из предоставленного файла. Не подставляй типовой договор вместо отсутствующего вложения.

## Запуск в AI Studio

Скрипт ниже — файл командной строки, не отдельный API-инструмент. Выбери `shell` из инструментов текущего ответа и передай ему команду `python3` с фактическим путём к скрипту и входным файлам, используя схему именно этого инструмента. Сначала прочитай этот SKILL.md и проверь пути через shell; затем выполни команду, проверь код завершения и прочитай результаты. Необходимость вычислений не создаёт новых инструментов. Если shell отсутствует, сообщи, что автоматический разбор не выполнен; текстовый разбор не выдавай за запуск скрипта. Если API завершил ответ ошибкой инструмента, сообщи об ошибке: такой ответ не подтверждает выполнение задачи.

```bash
python3 <skill-dir>/scripts/review_contract.py /app/contract.txt --out /app/contract_memo.md
```

Скрипт собирает фрагменты по темам и предварительные сигналы риска. Затем прочитай весь текст по смыслу: слово «ответственность» само по себе не подтверждает защиту стороны, а «не несёт ответственности» меняет смысл. Проверь отрицания, исключения, перефразировки, ссылки между пунктами и того, чьи интересы рассматриваются.
Каждая находка должна содержать номер пункта или точную цитату, конкретное последствие для указанной стороны и вопрос юристу. Разделяй факт текста, интерпретацию и отсутствие данных. «Не найдено по словарю» не равно «в договоре отсутствует». Не делай неподтверждённых выводов о незаконности или обязательствах. Результат — перечень для ручной проверки, не заключение о юридической безопасности.

Описывай попытку сторон установить условие («пункт предусматривает отказ от возмещения», «создаёт риск ограничения выхода»), а не гарантированное правовое последствие («заказчик лишён права», «не сможет взыскать»). Исполнимость зависит от применимого права и контекста. Если право и юрисдикция не указаны, не предполагай их по языку документа. Не добавляй номера статей или выводы судебной практики без предоставленного либо проверенного источника. Вопросы юристу можно сформулировать по смыслу условия без ссылок на непроверенные нормы.

## Данные и выполнение

Работай с текстом пользователя и загруженными файлами. Доступ к CRM, почте, 1С и другим корпоративным системам не требуется и не подразумевается.
Найди фактические файлы в контейнере; пути из примеров замени на реальные. Путь к скрипту определяется относительно прочитанного `SKILL.md`, а рабочие копии и результаты сохраняй отдельно в `/app` или `/tmp`.
Сначала посмотри заголовки и несколько строк. Если структура отличается, используй параметры сопоставления или подготовь нормализованную копию, показав соответствие полей. Не заменяй отсутствующие или нераспознанные значения нулями.
CSV читается как UTF-8, XLSX — локальным openpyxl из комплекта. Старый XLS требует экспорта в XLSX/CSV; смена расширения не конвертирует файл. Если вложение недоступно, сообщи, какой файл не найден; не анализируй вымышленные данные.
После запуска прочитай созданные файлы, сверь числа с входом и выдай краткий результат со ссылками на существующие артефакты. `manifest.json` содержит метрики и статус для следующего шага.

## Примеры

См. [examples.md](examples.md).

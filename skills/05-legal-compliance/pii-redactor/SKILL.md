---
name: pii-redactor
description: "Предварительно маскирует типовые персональные данные в копии TXT/CSV/XLSX и показывает найденные совпадения. Применяется перед ручной проверкой документа; полнота обезличивания не гарантируется."
metadata:
  source: Academy of Yandex AI Studio
  shelf: Legal & Compliance
  type: shell
  role: Специалист по защите данных
  tools: [shell]
  version: "0.2.0"
inputs: [document]
outputs: [redacted_document, audit_report]
---

# Предварительное маскирование персональных данных

```bash
python3 <skill-dir>/scripts/redact.py /app/input.txt --out /app/input.redacted.txt --report /app/masking_report.md --report-json /app/masking_report.json
```

Всегда работай с отдельной копией; не перезаписывай входной файл и не передавай его содержимое наружу. Правила распознают email, телефоны РФ, карты, ИНН, СНИЛС, паспорт РФ и IP. Текстовые значения XLSX проверяются на всех листах; формулы сохраняются. Числовые значения без совпадений не преобразуются в текст.
Выдай копию и отчёт с количеством совпадений. Перед передачей документа нужна ручная проверка пропусков и ложных срабатываний: имена, адреса, изображения, формулы, комментарии и метаданные могут остаться. Ноль совпадений не означает отсутствие персональных данных. Не называй результат гарантированно обезличенным.

## Данные и выполнение

Работай с текстом пользователя и загруженными файлами. Доступ к CRM, почте, 1С и другим корпоративным системам не требуется и не подразумевается.
Найди фактические файлы в контейнере; пути из примеров замени на реальные. Путь к скрипту определяется относительно прочитанного `SKILL.md`, а рабочие копии и результаты сохраняй отдельно в `/app` или `/tmp`.
Сначала посмотри заголовки и несколько строк. Если структура отличается, используй параметры сопоставления или подготовь нормализованную копию, показав соответствие полей. Не заменяй отсутствующие или нераспознанные значения нулями.
CSV читается как UTF-8, XLSX — локальным openpyxl из комплекта. Старый XLS требует экспорта в XLSX/CSV; смена расширения не конвертирует файл. Если вложение недоступно, сообщи, какой файл не найден; не анализируй вымышленные данные.
После запуска прочитай созданные файлы, сверь числа с входом и выдай краткий результат со ссылками на существующие артефакты. `manifest.json` содержит метрики и статус для следующего шага.

## Примеры

См. [examples.md](examples.md).
